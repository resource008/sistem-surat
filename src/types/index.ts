export type {
  DetailPI, DetailSurat, FormState, // ← tambah
  PIItem, // ← tambah
  RegisterPI, RegisterSurat, Role, SuratItem
} from "./surat-types"

export { EMPTY_FORM, EMPTY_PI_ITEM, EMPTY_SURAT_ITEM } from "./surat-types"; // ← tambah EMPTY_PI_ITEM
